{
  "name": "workspace",
  "version": "0.0.0",
  "packageManager": "pnpm@11.17.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "dev": "corepack pnpm --filter @workspace/slips-morocco run dev",
    "build": "corepack pnpm run typecheck && corepack pnpm --filter @workspace/slips-morocco --filter @workspace/api-server run build",
    "preview": "corepack pnpm --filter @workspace/slips-morocco run serve",
    "typecheck:libs": "tsc --build",
    "typecheck": "corepack pnpm run typecheck:libs && corepack pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "prettier": "^3.9.5",
    "typescript": "~5.9.3"
  }
}
